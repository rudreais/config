#include <stdio.h>
#include "maze.h"

void debug(maze_t *maze)
{
	printf("--------------debug--------------\n");
	printf("nb_set=%d\n", maze->rows->nb_set);
	for (int i = 0; i < maze->rows->nb_set; i++)
		printf("%d\n", maze->rows->size_set[i].length);
	printf("--------------end----------------\n");
}
