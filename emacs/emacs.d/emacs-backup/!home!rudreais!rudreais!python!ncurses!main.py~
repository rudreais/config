from curses import wrapper

def main(stdscr):
    # Clear screen
    stdscr.clear()
    stdscr.addstr(i, 0, '10 divided by {} is {}'.format(v, 10/v))
    stdscr.refresh()
    stdscr.getkey()

wrapper(main)
