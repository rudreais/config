/*
** EPITECH PROJECT, 2018
** stumper
** File description:
** parser
*/

#pragma once

#include "prototype.h"

void get_bytes(parser_t *parser, char *arg, int index);
void get_lines(parser_t *parser, char *arg, int index);
void get_quiet(parser_t *parser, char *arg, int index);
void get_quiet(parser_t *parser, char *arg, int index);
void get_verbose(parser_t *parser, char *arg, int index);

static char *opt[][2] = {
	{"-c", "--bytes"},
	{"-n", "--lines"},
	{"-q", "--quiet"},
	{"-q", "--silence"},
	{"-v", "--verbose"},
};

static void (*func[5])(parser_t *, char *, int) = {
	&get_bytes,
	&get_lines,
	&get_quiet,
	&get_quiet,
	&get_verbose
};
