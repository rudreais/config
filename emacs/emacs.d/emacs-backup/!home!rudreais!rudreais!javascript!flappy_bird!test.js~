function setup() {
    createCanvas(640, 480);
}

function draw() {
    if (keyPressed()) {
	fill(0);
    } else {
	fill(255);
    }
    rect(25, 25, 50, 50);
}

function keyPressed() {
    if (keyIsPressed && key == ' ') {
	return true;
    } else {
	return false;
    }
}
