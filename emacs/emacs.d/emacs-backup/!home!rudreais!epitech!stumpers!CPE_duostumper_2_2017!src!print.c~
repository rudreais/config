/*
** EPITECH PROJECT, 2018
** gol
** File description:
** print
*/

#include <stdio.h>
#include "gol.h"

void print_map(map_t *map)
{
	for (int i = 0; map->states[i]; i++) {
		my_putstr(map->states[i]);
		my_putchar('\n');
	}
}
